{
  "name": "shorten-url-benchmark",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-dep load harness for the shorten-url backend (uses node-postgres for short-code sampling only)",
  "type": "commonjs",
  "scripts": {
    "login": "node lib/login.js",
    "smoke": "node scenarios/read-only.js --concurrency 5 --duration 5",
    "bench": "bash run.sh"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
